@model IEnumerable<ePaperLive.DBModel.user_activity_log>

@{
    ViewBag.Title = "User Activity Log";
}

<div id="main">

    <!-- ======= Breadcrumbs ======= -->
    <section id="breadcrumbs" class="breadcrumbs">
        <div class="container">

            <ol>
                <li><a href="@Url.Action("index", "home")">Home</a></li>
                <li>@ViewData["Title"]</li>
            </ol>
            <h2>@ViewData["Title"]</h2>

        </div>
    </section><!-- End Breadcrumbs -->
    <!-- ======= Wizard Section ======= -->
    <section id="portfolio-details" class="portfolio-details">
        <div class="container">

            <div class="row gy-4">
                <div class="col-lg-12">
                    <!-- start:wizrd -->
                    <table  class="table table-hover table-responsive table-striped small" id="UALList" width="100%">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.EmailAddress)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Role)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IPAddress)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.SystemInformation)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.LogInformation)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.CreatedAt)
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                           
                        </tbody>

                    </table>
                    <!-- end:wizrd -->
                </div>


            </div>

        </div>
    </section><!-- End Wizard Section -->

</div>

@section scripts{
    <script>
        //$(document).ready(function () {
        //    $('#UALList').DataTable({
        //        "pageLength": 25
        //    });
        //});

        function formatDate(dateString) {
    // Extract the milliseconds from the date string
    var milliseconds = parseInt(dateString.substr(6));
    // Create a new Date object with the milliseconds
    var date = new Date(milliseconds);
    // Format the date as "YYYY-MMM-DD"
    var formattedDate = date.getFullYear() + '-' + 
                        date.toLocaleString('default', { month: 'short' }) + '-' + 
                        date.getDate();
    return formattedDate;
}

    $(document).ready(function () {
        $('#UALList').DataTable({
             dom: 'Bfrtip',
        buttons: [
           'excel', 'print'
        ],
        "pageLength": 25,
        "processing": true,
        "paging": true,
        "serverSide": true,
        "ajax": {
            "url": '@Url.Action("index", "ual")',
            "type": "POST",
            "dataType": "json",
            "data": function (d) {
                console.log(d);
                
                d.start = d.start || 0;
                d.length = d.length || 25; // This sets the default number of records to 10
            }
        },
        "columns": [
            { "data": "EmailAddress" },
            { "data": "Role" },
            { "data": "IPAddress" },
            { "data": "SystemInformation" },
            { "data": "LogInformation" },
             {
                "data": "CreatedAt",
                "render": function (data, type, row) {
                    return formatDate(data);
                }
            }
        ]
    });
});

    </script>
}